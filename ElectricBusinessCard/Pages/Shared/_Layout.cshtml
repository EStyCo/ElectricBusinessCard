<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title>@ViewData["Title"] - ElectricBusinessCard</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/bootstrap.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/ElectricBusinessCard.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg bg-light" data-bs-theme="light">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain" aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarMain">
                    <ul class="navbar-nav mx-auto">
                        <li class="nav-item mx-2">
                            <a class="nav-link @(Context.Request.Path.StartsWithSegments("/") ? "active" : "")" asp-page="/Home/Index">Главная</a>
                        </li>
                        <li class="nav-item mx-2">
                            <a class="nav-link @(Context.Request.Path.StartsWithSegments("/Works") ? "active" : "")" asp-page="/Works/Works">Услуги</a>
                        </li>
                        <li class="nav-item mx-2">
                            <a class="nav-link @(Context.Request.Path.StartsWithSegments("/Price") ? "active" : "")" asp-page="/Price/Price">Прайс</a>
                        </li>
                        <li class="nav-item mx-2">
                            <a class="nav-link @(Context.Request.Path.StartsWithSegments("/Contacts") ? "active" : "")" asp-page="/Contacts/Contacts">Контакты</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <div class="container">
        <main role="main" class="my-2 bg-light rounded-3" style="position: relative; overflow: hidden;">
            @RenderBody()
        </main>
    </div>

    <footer class="bg-light py-3 text-center">
        <div class="container">
            <p class="mb-1">©2025 Электро-Сервис. Все права защищены.</p>
            <p class="mb-1">Электромонтажные работы в Иркутске.</p>
            <p class="mb-0">
                Телефон: <a href="tel:+79140003944">+7 (914) 000-39-44</a>
            </p>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>